# Current State

Last updated: 2026-08-08

## Summary

HORUS is a company being founded. HORUS V1 is the first internal operating system built for it, used by the founder to acquire HORUS's first client.

The repository contains the approved product documents, locally cached calibration evidence, and an executable Electron/React/TypeScript foundation. HORUS V1 is fully specified end to end in `PROJECT_CHARTER.md`; its local SQLite and immutable-evidence foundation is implemented and tested. Phase 5 has completed the first real, approval-gated prospect run, including a public source-bounded concept and operator-confirmed manual outreach.

Thirty real businesses have now calibrated the models. The operator approved retaining `reputation-scoring-v1` and `web-opportunity-v2` unchanged; the remaining unvalidated assumptions are recorded below rather than treated as settled.

**DEC-046 through DEC-062 approved by Javier Napoles, 2026-08-07**, closing out Phase 6 steps 1–3 as accepted history rather than proposals awaiting review.

**DEC-063 and DEC-064 approved by Javier Napoles, 2026-08-07.**

**DEC-068 — proposed 2026-08-07, awaiting operator review.** `src/domain/reputation-scoring.ts` implements charter section 9 (`reputation-scoring-v1`) as real code for the first time — six gates, all five scoring factors, the 9.6 missing-data principle, and the 9.4/9.5 objective-auto-reject-vs-judgment-flag split (see DEC-068 for the interpretive choices this required, in particular G4). Verified against all six of charter 9.3's reference profiles and against three real calibration numbers already published above (Tuff Lawn, CINCO DE MAYO, Teed and Brown review-volume points). Verified in an isolated sandbox copy of the repository (never touching the mounted repository's real `node_modules`, built for macOS): lint clean, both `tsc` configs clean, `vite build` clean, and **99 of 99 tests passing** — the 83 tests present as of DEC-067 plus 16 new reputation-scoring tests, none regressed. That run was on a different OS/architecture than the operator's machine, so a first `npm run lint && npm run test && npm run build` on the real macOS machine is still the one check this decision could not itself perform. This resolves DEC-050 for reputation-scoring specifically; `run_deterministic_scoring` is not yet wired to call it, and the historical Finescape/SEASONS EATS/calibration figures are not yet reproduced from raw evidence through this implementation — both remain open follow-ups.

**DEC-097 — accepted 2026-08-09 (operator-directed, "continua").** `web-opportunity-v2`'s largest factor — mobile responsiveness, 30 of 100 points — is measured for the first time, **from evidence already retrieved**. DEC-072 left it `unmeasured` saying it "requires a rendered inspection; not yet wired". The rendered inspection was already happening: every PageSpeed call returns a full mobile Lighthouse run and only `audits.interactive` had ever been read from it. This matters because web opportunity is what *orders* the shortlist, so its biggest factor being absent meant ranking was driven by whichever factors were cheap to measure — the exact drift DEC-034 warned about. `src/domain/mobile-responsiveness.ts` (new, pure) maps `viewport` / `content-width` / `tap-targets` to the model's existing three-valued type; the IPC carries a trimmed slice and the renderer interprets it, preserving the measure-in-main/score-in-renderer layering. **No new request, no new credit, no model change** — no parameter moves, so DEC-007/DEC-011's versioning rule is not engaged. Charter 10.4 governs the missing case: an absent audit is `unmeasured`, never a failure, because a dropped audit read as a failure is worth 30 points in the direction that flatters the prospect. 19 new tests bring the suite to 381. **A mutation this suite missed on its first pass is recorded in the decision:** treating an unparseable audit score as a failure passed all 17 original tests — Lighthouse emits a null score with no display mode when an audit *errored*, which is a check that did not happen, not a site that failed one. Two tests added; the mutation now fails. **Still `unmeasured`:** broken elements, commercial ineffectiveness, and six of seven obsolete-appearance indicators — those need a link crawl and a content review, genuinely new work rather than an unread field.

**DEC-096 — accepted 2026-08-09 (operator-directed, "ok continua hasta terminar").** DEC-031's 60-day demonstration review, which DEC-090 left as a mechanism with nothing to trigger it. Implementing it surfaced a second, unstated gap: DEC-031 exempts an engaged prospect, and **nothing in HORUS could record that a business had responded** — the tracker knew about publication, handoff, declared send and follow-up, but a reply from the business itself had nowhere to go. `reviewDemonstrations` computes `live` / `expired_awaiting_decision` / `engaged` / `removed`; a new `outreach.response_recorded` event and IPC channel let the operator declare a reply; `TrackerEntry` gains `respondedAt` and `removedAt`. Three DEC-031 clauses are enforced rather than approximated: **nothing is taken down automatically** (the function returns a question, and a test asserts the shape carries nothing a caller could mistake for consent); **the prompt repeats**, derived on every read so an ignored one stays visible; and **an engaged prospect is out of scope**, which is why the response event had to exist. `now` is a parameter for the same reason as in DEC-089 — elapsed wall-clock time must not be reproducible against a stored timestamp. 15 new tests bring the suite to 362; both mutations caught. One existing DEC-082 test broke on the new fields and was **updated rather than loosened** — its deep-equality assertion is what caught the change. **Still open:** DEC-031 also asks that a removed URL serve a neutral unavailable page not naming the business, and that a removal reason be retained; `wrangler pages project delete` just stops resolving, and no reason is captured.

**DEC-095 — accepted 2026-08-09 (operator-directed, "ok continua hasta terminar").** Charter 15's other half, which DEC-089 named as the part it was not building: stale evidence can now be refreshed, **and the operator is shown what changed**. The showing is the point — a refresh that silently replaced the old figures would satisfy the letter of the freshness rule and defeat its purpose, since the operator would proceed on fresh data without learning that the rating fell while they were deciding. `src/domain/evidence-diff.ts` (new, pure) compares two retrievals and flags each difference as material or not: a fallen rating or review count is material, a risen one is reported but not flagged; a changed name or address is material and names G6 explicitly, because charter 9.5's identity question has arrived unbidden; a website appearing where there was none is material because it changes the web-opportunity premise the prospect rests on. **A flag for attention, never a decision (DEC-008)** — a test asserts the returned shape has exactly three keys so a verdict cannot be quietly added. A business that has vanished from a fresh search of its own category is reported as a finding and deliberately not interpreted. The control states its credit cost, and the interface warns that displayed scores were computed from the earlier retrieval and have **not** been recalculated. 18 new tests bring the suite to 347; both mutations caught, including dropping `forceRefresh` so the refresh would have silently served DEC-077's cache. **Still open:** rescoring is not automatic, and the refresh reaches the discovery listing only — a reputation score built on old reviews is not refreshed by this.

**DEC-094 — accepted 2026-08-09 (operator-directed, "ok continua hasta terminar").** The operator's judgment is now durable, closing the follow-up DEC-091 recorded against itself. **Why it matters more than convenience: a judgment is the one input to a reputation score that cannot be recomputed from retained evidence.** Rerunning the model reproduces every factor and objective gate to the decimal (DEC-086, DEC-087), but nothing can reconstruct what the operator concluded from reading the reviews — so losing it makes a qualification decision irreproducible, the one property charter 9.7 and DEC-020 exist to guarantee. `src/domain/judgment-log.ts` (new, pure) projects `prospect.judgment_recorded` events into the latest judgment per listing; two IPC channels write and read it; `CandidateScoreAction` restores prior judgment on mount and records through an explicit button, not as a side effect of typing. **A later judgment supersedes without erasing** — the earlier event stays in the log and the projection counts revisions, so a change of mind stays visible, mirroring the rule the decision log itself follows. Three properties enforced with tests rather than assumed: ordering derived from `occurredAt` rather than trusting `listEvents`; a malformed payload skipped rather than guessed at, and **a malformed later event cannot clear a good earlier one**; and an unanswered draft round-trips as unanswered, so DEC-091's default-is-never-a-pass survives storage. 14 new tests bring the suite to 329; both mutations caught. **Still open:** the rest of the working prospect state — scores, audits, the selection — remains in memory, and the judgment is not yet shown in the tracker.

**DEC-093 — accepted 2026-08-09 (operator-directed, "continuemos").** Interaction tests against a real DOM, closing the layer DEC-084 named and deferred twice. The justification is DEC-092's: **both structural dead ends found in this session were invisible to every test the project had**, because both lived in the wiring *between* units while 305 tests passed. Four dev dependencies (`jsdom`, `@testing-library/react`, `@testing-library/user-event`, `@testing-library/jest-dom`), jsdom selected per-file so existing node tests are untouched. Ten tests cover what the other layers cannot reach: an unqualified candidate renders its reason and **no** route forward; a qualified one renders the button and calls back with the right id; the freshness verdict appears before either gate; generating a preview leaves the publish gate shut; ticking approval opens it; **stale evidence keeps it shut even with approval ticked**; the publish IPC channel is never invoked through a shut gate; and the preview iframe carries `noindex` and an empty `sandbox`. 315 tests. All three mutations were caught — the first produced the failure message *"a real publish was attempted through a shut gate"*, a stubbed publish channel called once, which is a test observing the exact failure DEC-004 exists to prevent. **Still uncovered:** the Electron main process and preload bridge (jsdom stubs `window.horus`), real keyboard traversal and focus appearance, and the outreach gate, which renders only after a real publication.

**DEC-092 — accepted 2026-08-08 (operator-directed, "empecemos a validar entonces").** **The real pipeline ran end to end for the first time in the project's history.** `scripts/validate-pipeline.ts` (`npm run validate:pipeline`) drives the same functions `main.ts` wires to IPC, from a real SerpApi search through to a demonstration preview, and stops — DEC-004's gates are unreachable from it. 10 credits: 1 search of landscaping/Norwalk returning 10 candidates, 9 review-history pages across 3 of them.

**Two blockers were found before a single credit was spent.** `latitude`/`longitude` were absent from `config/local.json` (optional since DEC-074, never filled in), so every candidate was excluded with `no_proximity_data` — **a second dead end stacked directly behind DEC-091's**, and only one is visible at a time. Approximate coordinates (Stamford centroid, already public in DEC-035) were written with a note in the file that they are validation-only and **must be replaced before any real prospect decision**, since the 5/15/30-mile bands are the first ranking key. Second: the application had never persisted anything, under any name — every piece of real evidence in this repository until now was produced outside it.

**What the run proved.** The model is stable on live data: TwoGen recomputed to **71.0** against the **71.1** DEC-073 recorded four days earlier; Tuff Lawn to 76.2 against the calibration's 73.6. DEC-091's fix works where it matters — judgment unanswered ranked 0 of 3, answered ranked 2. LT Landscaping stayed excluded *even with judgment answered* because it failed G3, the objective recency gate: answering judgment gates does not rescue an objective failure, which nothing had previously tested. DEC-077's cache served a second pass with no credit, validated live for the first time. DEC-089's freshness reported correctly. The demonstration generated with zero placeholders, `noindex` and concept notice present.

**One new finding.** All three candidates retrieved exactly 28 reviews over 3 pages, `paginationExhausted: false` — DEC-018's cap, hit every time. For Tuff Lawn that is **28 of 314 published reviews, a 9% sample**. Reported honestly as `partial_data`, but recency and consistency for a high-volume business rest on a thin slice, and the cap was set for cost rather than as a sampling decision. Recorded, not acted on — changing it changes the model.

**Not validated:** the React event wiring (the harness bypasses the interface), both DEC-004 gates, web-opportunity measurement (stood in at a fixed value), and proximity accuracy while the coordinates remain a centroid. Evidence: [`checkpoints/2026-08-08_phase-6-first-real-pipeline-run.md`](checkpoints/2026-08-08_phase-6-first-real-pipeline-run.md).

**DEC-091 — accepted 2026-08-08 (operator-directed, design delegated).** The operator can finally answer charter 9.5's three judgment gates (G4 complaint pattern, G5 operational status, G6 listing identity), **which is what makes qualification reachable at all.** DEC-071 hardcoded them to `insufficient_data` and built no interface to answer them; since `qualified` requires every gate passed, and `buildShortlist` excludes anything unqualified, and "Select as prospect" renders only for ranked entries, **the shortlist was permanently empty and everything downstream — prospect record, demonstration, publication, outreach, tracker — was unreachable from real data.** Verified by running `buildShortlist` against a strong candidate: `ranked: 0`, `excluded: ["not_reputation_qualified"]`. Nothing had caught it because none of that work was ever exercised live. `src/domain/operator-judgment.ts` (new, pure) plus three controls that rescore from retained history at no further credit cost. The default is never a pass, and a verdict with a blank rationale is refused rather than downgraded. A test asserts the whole arc: same candidate, `qualified: false` and empty shortlist → `qualified: true` ranked 1. Answering against the business still blocks. 13 new tests bring the suite to 305; both rules confirmed against mutations. **Still open:** the judgment is component state and is lost when the app closes, though charter 14 says the rationale belongs in the record; and `pattern_found` fails the gate rather than surfacing as the DEC-008 operator flag.

**DEC-090 — accepted 2026-08-08 (operator-directed, "quiero que termines el mvp").** Charter 15's removal path, which did not exist. DEC-080 could put a page about a real business on the public internet and the application offered **no way to take it down** — Phase 5's Finescape concept was retired by typing Wrangler commands outside the app. `removeDemonstrationSite` runs `wrangler pages project delete` (the whole project, since disabling one deployment leaves earlier ones reachable), behind a new IPC channel that records a durable `demonstration.removed` event. The control requires the operator to **type the project name exactly** — Wrangler's interactive confirmation is not skipped by `--yes`, it is moved into HORUS — guarded both in the `disabled` attribute and inside the handler, and the interface states plainly that removal cannot be undone from the app. 8 new tests bring the suite to 292; the guard was confirmed against a mutation. **Not exercised against a live Cloudflare project** — the SEASONS EATS concept remains published and untouched. **Still not built:** DEC-031's 60-day prompt that would surface an expired demonstration for a removal decision; this supplies the mechanism it would trigger.

**DEC-089 — accepted 2026-08-08 (operator-directed, "quiero que termines el mvp").** The charter's 30-day freshness rule, enforced at both DEC-004 gates for the first time. It was specified in charter 14/15 — stated twice, as a hard requirement at publication and at outreach — and **implemented nowhere**: a search found it only in comments. DEC-080/081 built real publication and a real Gmail handoff without it, so the MVP would have published a public page about a real business, and drafted a message to its owner, from evidence of any age. `src/domain/freshness.ts` (new, pure) plus guards on both gate buttons, with a banner and a stated reason, and the assessment shown in the prospect header before either gate is reached. **This is the only module in the domain that reads the current clock, deliberately** — scoring must be reproducible against its stored timestamp (charter 9.7), freshness must not be, because the question is whether the evidence is still current *today*. Missing, unparseable, and future timestamps all block; a clock error must not open a contact gate. 26 new tests bring the suite to 284, and the tests caught a real bug during wiring: an unscored prospect produced `undefined`, which was read as evidence-without-a-timestamp and blocked with a false reason while the listing timestamp was fine. Both guards confirmed against mutations. **Still missing:** the refresh-and-show-what-changed flow charter 15 also requires — the operator is told the evidence is stale, but re-running discovery to refresh it is a separate, unbuilt step.

**DEC-088 — accepted 2026-08-08 (operator-directed, "continua").** The Phase 6 security and prompt-injection review, a named roadmap item never previously done. See [`SECURITY_REVIEW.md`](SECURITY_REVIEW.md). Six findings, two fixed, one left open on purpose. **Fixed:** a redirect escaped the hostname denylist entirely — `redirect: 'follow'` let fetch resolve the chain with no re-validation, so a public https URL answering `302 Location: http://10.0.0.5/` was fetched against the operator's own network and the result still reported the *requested* address; and the documented response-size cap ran after `response.text()`, buffering a 2 GB body whole before slicing it. Also hardened the screenshot `BrowserWindow` — the only window that renders and executes a third party's page — which declared only `offscreen: true` and inherited the rest from Electron defaults that happen to be safe today. **Left open, and the most important one:** the analyst can read retained evidence and fetch an arbitrary public URL, so hostile review text describing `https://attacker.example/?d=<snapshot>` is a working exfiltration channel that happens in the request, before any output parsing. The recommended hostname allowlist is a design trade-off (it conflicts with legitimate `example.com → www` redirects) and is recorded for the operator, not imposed. 12 new tests bring the suite to 258; 11 were confirmed to fail against the previous implementation. **The roadmap item is closed only in part** — subscription limits, product terms, a dependency audit, the unexercised publish/Gmail paths, and the complete absence of any retention or deletion policy for retained third-party business data are all still open.

**DEC-087 — accepted 2026-08-08 (operator-directed, "continua").** The Phase 5 companion to DEC-086. **Both figures reproduce:** Finescape recomputes to 48.11 against a published 48.1, and SEASONS EATS to 73.06 against 73.06 — exact to the hundredth. These two matter more than any calibration number: one is why a published concept was retired, the other is why the project's only real prospect was approved for a public concept and outreach to a real business owner. Both were computed before the model existed in code; both now rest on code that regenerates them. Two `AGENT_ARCHITECTURE.md` §11 acceptance criteria are asserted on their deterministic side, including the important one — SEASONS EATS scores *above* the 70 threshold and is still `qualified: false`, because G4/G5/G6 need an operator, not a model. Confirmed against two mutations before being trusted: dropping the threshold to 45, and making G4 pass by default instead of `insufficient_data`. `npm run phase5:reproduce` prints it. 7 new tests bring the suite to 246. **`ROADMAP.md` is also brought back in line:** its Phase 6 checklist had every box unchecked while four were demonstrably done; those four are now ticked against their decisions, with a dated note explaining the drift. Two items are flagged for the operator rather than resolved — whether DEC-079–081 building concept and outreach composition satisfies the box requiring it come only after the analyst replay passes evaluation, and the Phase 6 exit criterion's required retain/revise/reject decision on the agent runtime, which nothing in the decision log records.

**DEC-086 — accepted 2026-08-08 (operator-directed, "ok").** The calibration figures below have been reproduced from raw evidence through the real `reputation-scoring-v1` code for the first time, closing the open follow-up DEC-068 recorded. Every number in this document's calibration section was computed on 2026-08-05, when the model existed only in the charter; four later conclusions rest on them. **They reproduce.** The four businesses labelled *complete data* match exactly — Tuff Lawn 73.6, CINCO DE MAYO 68.3, United Sewer & Water 65.6, Barcelona Wine Bar 65.4. The two labelled *partial* come out higher (Mashed Burgers 83.3 vs 68.3, FAIRCONN 77.3 vs 65.0), and each delta equals that business's Factor-4 award **to the decimal** — 15.0 and 12.3 — which is exactly the conservative floor this document already records for partial histories ("Factor 4 at zero"). No unexplained residue anywhere in the model. Costs nothing: it reads the 230 already-cached files (DEC-020). `npm run calibration:reproduce` prints it; `tests/calibration-reproduction.test.ts` asserts it every commit, and was confirmed to catch a mutation of the 400-review saturation point before being trusted. 10 new tests bring the suite to 239. **Two limits:** the harness reconstructs 54 businesses rather than the curated 30, so the aggregate claims (22 complete histories, range 49.5–85.1, median 67.0) are *not* reproduced — only the six named totals are; and its own `status` column reads `partial_data` throughout for a harness reason (it derives pagination exhaustion from whether a further page was offered), so it should not be read as the 2026-08-05 complete/partial classification.

**DEC-085 — accepted 2026-08-08 (operator-directed, "ok hazlo y continua", again scoped away from DEC-004's gates).** `App.tsx` fell from 921 lines to 111. Eight modules under `src/views/` hold the components every increment from DEC-069 onward had been appending to one file. Nothing about any component's markup, props, state, or behavior changed — the extraction sliced exact line ranges rather than retyping, and **the refactor was verified by pixel comparison: the rendered application before and after produced screenshots with identical SHA-256 hashes.** The payoff is concrete and smaller than predicted: DEC-084's rendered layer now reaches `ProspectRecord`, where three new checks assert that an unscored, unmeasured prospect shows "not yet scored" and "not yet measured" rather than a zero (hard rule 6 and charter 9.6, executable on a real component for the first time). Both DEC-004 gates stay out of reach — they render only after a preview or draft exists, which `renderToStaticMarkup` cannot drive. DEC-084's static layer was also fixed: it had been pinned to the path `App.tsx` and failed when the markup moved, the right failure for the wrong reason; it now globs every `.tsx` under `src`. FUNCTIONAL_DESIGN §6's six named views still do not exist as views — this makes that restructuring possible without claiming to have done it. 3 new tests bring the suite to 229.

**DEC-084 — accepted 2026-08-08 (operator-directed, "confío en tu criterio" — choice of next work delegated, deliberately scoped away from DEC-004's gates).** Defines the accessibility verification method DEC-038 deferred to Phase 3 and Phase 3 never supplied. `tests/accessibility.test.tsx` covers five of DEC-038's six properties in three explicitly unequal layers — rendered (`react-dom/server`, no new dependencies, default view only), static (regex over `App.tsx`, reaches the surfaces rendering cannot), and stylesheet (the focus rule). Combined with DEC-083's contrast test, DEC-038 is now enforced rather than claimed. 11 new tests bring the suite to 226. Each new check was confirmed to fail against a deliberate mutation before being trusted — a removed `alt`, an `onClick` moved to a `<div>`, a focus ring stripped of its replacement, a DEC-004 gate stripped of its textual blocked-reason. **Still not covered, and stated plainly in the decision:** real keyboard traversal and real focus appearance in a browser, and the rendered layer cannot reach `ProspectRecord`, either DEC-004 gate, or the evidence panels, since they are private to `App.tsx`. Closing that needs either `jsdom` + `@testing-library/react` (rejected for now — two dependencies plus exporting private components for tests, which belongs to the `App.tsx` restructuring pass) or a real browser harness.

**DEC-083 — accepted 2026-08-08 (operator-directed, "lets do it").** The first deliberate design work on the operator interface, and the first supersession of a visual decision. An analysis of whether a dark, more atmospheric direction could sit inside DEC-036 found that DEC-036 never specified luminance at all — the word "light" appears nowhere in it, in FUNCTIONAL_DESIGN §8, in DESIGN_REFERENCES, or in charter §18 — so "neutral foundations" constrains chroma, not brightness, and the light palette in `index.css` had been reading as decided when it never was. What genuinely needed superseding was the single word "editorial", the register. DEC-083 adopts a dark neutral instrument-panel register under six binding rules, retains every DEC-036 principle and exclusion, and leaves DEC-037 and DEC-038 untouched. `index.css` becomes a documented token layer and `App.css` a structured component layer; the panels appended by DEC-069 through DEC-082 — including both DEC-004 approval gates, which had been rendering as unstyled default markup — get their first designed treatment. Two rules do real work beyond appearance: no quantity fills a gauge or bar (so a low score and `insufficient_data` cannot become the same picture — the DEC-073 case, 71.1 against a 70 threshold and correctly not qualified, is the live precedent), and every disabled control states why it is disabled in words, since on a dark surface a dimmed control reads as absent rather than blocked. Machine-produced values are now monospace and operator prose sans, so provenance is legible before the value is read. **Contrast is enforced, not claimed:** `src/design/contrast.ts` (new, pure — WCAG 2.1 relative luminance and contrast ratio) plus `tests/contrast.test.ts` assert every token against the surfaces it is actually used on, and this partly discharges DEC-038's deferred "Phase 3 defines the technical verification method" for colour. That check found a real defect on its first run — see the note below. 25 new tests bring the suite to 215. Verified on the operator's own macOS machine: lint clean, 215/215 tests, `tsc -b`, `vite build`, and `tsc -p tsconfig.electron.json` all clean, and the built renderer visually confirmed in a real Electron window.

**One correction during DEC-083, recorded because the check catching it is the point.** The first contrast run failed: `--focus-ring` measured 1.38:1 against `--action-fill`, far below the 3:1 that WCAG 1.4.11 requires. The design was not at fault — the focus ring is two-tone (`0 0 0 2px --surface-0, 0 0 0 4px --focus-ring`), so the dark inner ring sits between the light outer ring and the light button fill at 15.3:1, and the light-on-light pair the test measured is never actually adjacent. The assertion was corrected to follow the real adjacency chain (fill→inner, inner→outer) rather than loosened, and the reasoning is recorded in the test itself so a future reader does not "simplify" it back into the wrong check.

**DEC-082 — accepted 2026-08-08 (operator-directed, "seguimos").** The tracker — charter §4's last unbuilt step. `persistence.ts` gained `listEvents`, reading back the durable events DEC-080/081 already wrote; `src/domain/tracker.ts` (new, pure) projects `demonstration`/`outreach`/`follow_up` events into one row per prospect, later events overwriting earlier ones of the same kind. A new `follow_up.scheduled` event records only an operator-supplied date and note (DEC-030 — HORUS never schedules or infers one). `ProspectRecord` gained a follow-up form after declaring outreach sent; a new collapsed `RealTrackerPanel` lists every tracked prospect, reconstructed fresh from the event log each time rather than a second stored copy (charter §14). Charter §4's full workflow — search through tracked follow-up — now exists end to end for the first time. 7 new tests bring the suite to 190. Not yet exercised live.

**DEC-081 — accepted 2026-08-08 (operator-directed).** Real outreach composition and the second DEC-004 gate — charter §4's full loop (search → qualify → shortlist → select → demonstrate → publish → outreach) now exists end to end. `src/domain/outreach.ts` (new, pure) builds a subject/body from only the business's own name, category, and real published demo URL — no claims about the business. DEC-027 language selection is honestly left at English-only, with a stated reason (no review/reply text is retrieved anywhere in the codebase to evidence Spanish). `main.ts` wires the existing, previously-unused `buildGmailComposeHandoff` to a real `shell.openExternal` call plus a durable event, and a separate "declare sent" action records the operator's own send declaration (charter 17.3 — never inferred). `ProspectRecord` gained editable to/subject/body fields (recipient email never invented, always operator-entered) and a second, distinct approval checkbox from the publish one. 5 new tests bring the suite to 183. Not yet exercised live — same reasoning as DEC-080.

**DEC-080 — accepted 2026-08-08, after two rounds of clarification.** Real Cloudflare Pages publication, the first action anywhere in this session with a real, public, lasting consequence. `electron/publish-ipc.ts` (new) shells out to the operator's already-authenticated Wrangler CLI (`wrangler pages project create` then `wrangler pages deploy`), reusing the exact `SpawnImpl` contract already built and tested for the Claude Code runtime. Chosen over calling Cloudflare's REST API directly after research found that route requires an only-partially-documented manifest/hash/JWT flow internal to Wrangler. `ProspectRecord` gained a DEC-004 approval checkbox — disabled until checked — directly under the DEC-079 preview iframe; only on success is a durable `demonstration.published` event recorded via SQLite (the one thing in this whole feature arc that isn't in-memory-only, since a live URL is a real consequence). 8 new tests bring the suite to 178. **Not yet exercised live** — deliberately not run against the operator's real Cloudflare account without their explicit go-ahead in the moment.

**DEC-079 — accepted 2026-08-08 (operator-directed).** The first real demonstration-site generator: `src/domain/demonstration.ts` (new, pure) builds a single-page static HTML preview from a selected prospect's already-verified fields only (name, category, address, the new `phone` field, website, Google rating/review count) — bracketed placeholders for anything missing, no invented services/testimonials/pricing, no internal scores leaked into the page, DEC-024's notice banner and `noindex` unconditional. `ProspectRecord` gained a "Generate demonstration preview" button rendering the result in a sandboxed iframe. Not published, not sent, not the DEC-004 approval gate — a preview only. 7 new tests bring the suite to 170. Not yet exercised live.

**DEC-078 — accepted 2026-08-08 (operator-requested).** A "Capture website screenshot" button in `ProspectRecord` loads the candidate's site in a hidden Electron window (`electron/website-screenshot.ts`, new, `createWindow` injected so the module stays testable without a real Electron process) and shows the resulting image inline — no paid screenshot service, no new dependency, no new credential. Shared https-only/local-hostname URL validation extracted into `electron/agent/url-safety.ts` (mirrors `website-inspector.ts`'s existing check; that file is left untouched). In-memory only, same as DEC-076: never stored, explicitly labelled a reference aid rather than evidence. 6 new tests bring the suite to 163.

**DEC-077 — accepted 2026-08-08 (live finding + operator request).** Two fixes to real discovery search. First, a live run set "maximum examined" to 5 and got 20 candidates back — SerpApi's Google Maps engine doesn't reliably honor `num`; `discovery-ipc.ts` now slices the returned candidate list to `maxExamined` itself. Second, the operator asked that a repeat search for the same category+city not spend another SerpApi credit: `persistence.ts` gained `findLatestRawSnapshot`, raw evidence is now stored with a structured `{ requestUrl, category, city }` request, and `runRealDiscoverySearch` checks for a cached match before spending a new credit, unless the operator checks "force a fresh search." No freshness expiry — consistent with DEC-021, browsing/ranking may use cached data of any age. Older snapshots (stored before this decision) never match the new structured lookup, so caching starts working from now on, not retroactively. 8 new/changed tests bring the suite to 158 before DEC-078.

**DEC-076 — accepted 2026-08-08 (operator delegated, "haz lo que parezca mejor").** Prospect selection: `ProspectRecord` (new component in `App.tsx`) consolidates one selected shortlist entry's already-measured evidence — raw listing fields, full reputation gates/factors, full web-opportunity factors, proximity — into a single read-only view, reached via a new "Select as prospect" button on each `ShortlistView` row. In-memory only: nothing is written to SQLite or any other persistence, and the view says so on screen, along with stating plainly that selection is not an approval, publication, or outreach action. No new pure domain logic, so no new tests; suite stays at 152, same isolated-sandbox verification (lint, both `tsc` configs, `vite build` all clean). Not yet live-tested, same as DEC-074/075.

**DEC-075 — accepted 2026-08-07 (operator delegated, continuing MVP-first).** Real shortlist ranking: `src/domain/shortlist.ts` (new, pure) implements DEC-013/DEC-017's rule — reputation qualifies, proximity band ranks first, web-opportunity ranks within band, reputation breaks ties — over whatever candidates the operator has already scored by hand via DEC-071/072's per-candidate actions. A candidate missing any of the three inputs is listed as "not yet rankable" with a specific reason, never silently dropped or guessed. Deliberately does not auto-score every candidate (would spend a review-history + PageSpeed request per candidate without the per-action consent DEC-069/071/072 require). `App.tsx` lifts per-candidate score state up to the parent so `ShortlistView` can read it live. 8 new tests bring the suite to 152. Not yet live-tested, same as DEC-074.

**DEC-074 — accepted 2026-08-07 (operator delegated, prioritizing MVP wiring over UI/UX for now).** Real proximity: `src/domain/proximity.ts` (new, pure — haversine distance, the charter's provisional 5/15/30-mile bands, a band comparator) uses `gps_coordinates` SerpApi already includes on every discovery candidate — no extra retrieval or cost. Home base coordinates are a new, optional `config/local.json` field (`getHomeBaseCoordinates`, `null` when unset, backward-compatible with every existing config file); only the coordinate pair reaches the renderer, never the street address. `compareProximityBands` exists but nothing yet calls it to order a real shortlist — DEC-017's full ranking remains unbuilt; this supplies its first real input. 20 new tests bring the suite to 144. **Not yet verified live**, unlike DEC-069–072 — deferred at the operator's request to keep moving through MVP pieces.

**DEC-073 — accepted 2026-08-07, live verification of DEC-069 through DEC-072.** The operator ran the real packaged app (`npm run dev`) and worked the new panels directly, spending real SerpApi and PageSpeed credits for the first time. A real 20-candidate landscaping/Norwalk search stored real evidence; G1/G2 screening displayed correctly per candidate, including correctly suppressing measurement for a no-website listing; TwoGen Landscaping scored a real 71.1/100 reputation with G1–G3 passed and G4–G6 correctly `insufficient_data` — **`qualified: false` despite exceeding the 70 threshold**, the first live confirmation the operator-judgment gates actually withhold qualification rather than being silently satisfied; the same candidate's real PageSpeed measurement scored 8.2/12 performance with the rest honestly `unmeasured` and a real `tel:` link surfaced as an unscored supplementary finding. Nothing found contradicted DEC-069–072's design; no code change followed. Not yet exercised live: multi-page review-history pagination and a real failure path (rejected request, missing config).

**DEC-072 — accepted 2026-08-07 (operator chose directly from a menu of next-step options).** `web-opportunity-v2` now gets two real, mechanical factors per candidate — real PageSpeed performance (`electron/integrations/pagespeed.ts` gains `executePageSpeedMobile`; `electron/web-opportunity-ipc.ts`, new) and the `no-https` obsolete-appearance indicator (known from the URL's own scheme, no network call needed). A `tel:`-link check (reusing DEC-066's `inspect_public_website_readonly`) is shown as supplementary evidence, not folded into `brokenElements` — one regex match isn't the link crawl that factor represents. Mobile responsiveness, the other six obsolete-appearance indicators, a real broken-link crawl, and commercial ineffectiveness stay honestly `unmeasured`, so the resulting `scoreLowerBound` is a genuine lower bound. 8 new tests bring the suite to 131, same isolated-sandbox verification as DEC-068–071. No real PageSpeed request was made.

**DEC-071 — accepted 2026-08-07 (operator delegated in chat).** Review-history retrieval closes the loop DEC-050 opened: `electron/integrations/serpapi.ts` gains a `google_maps_reviews` request pair, `electron/review-retrieval-ipc.ts` (new) paginates it up to 3 pages per candidate with immutable per-page evidence storage, and `src/domain/review-history.ts` (new, pure) turns retrieved reviews into `reputation-scoring-v1`'s recency/consistency inputs. The renderer's new `CandidateScoreAction` (per candidate, in the real discovery panel) now runs a real, evidence-backed `buildReputationScore` — G4/G5/G6 stay `insufficient_data` by design, since only the operator can assess a complaint pattern, operational status, or listing identity (DEC-008); `qualified` therefore correctly waits on the operator even when every retrievable factor looks strong. 18 new tests bring the suite to 125, same isolated-sandbox verification as DEC-068–070. No real SerpApi credit spent; the first real end-to-end run is the operator's own next step.

**DEC-070 — accepted 2026-08-07 (operator delegated in chat).** `screenListingGates` (new, in `src/domain/reputation-scoring.ts`) checks only G1 and G2 — the two charter 9.1 gates answerable from a bare discovery listing's rating and review count — and the real discovery panel (DEC-069) now shows them per candidate. It is explicitly not qualification: G3–G6 and all five scoring factors need review-history retrieval, a separate, not-yet-built, credit-costing step. 1 new test brings the suite to 112, same isolated-sandbox verification as DEC-068/DEC-069.

**DEC-069 — accepted 2026-08-07 (operator delegated in chat).** Real SerpApi discovery is now reachable from the interface — `electron/config.ts` (new) reads `config/local.json` for the first time anywhere in the codebase, `electron/discovery-ipc.ts` (new) wires the existing, previously-unused `executeSerpApiDiscovery` to a real IPC channel with raw-evidence storage, and `App.tsx` gained a new, separate "Real discovery search" section — deliberately not a change to the Phase 4 representative workflow's `SearchStage`, which keeps its existing fixture-only behavior and its "not executed here" notice exactly true, per the operator's explicit choice between the two options presented. Scope is stated on-screen: one request, no pagination to `TARGET_QUALIFIED`/`MAX_EXAMINED`, no G1/G2 gating, no scoring — those remain open. 12 new tests (`config.test.ts`, `discovery-ipc.test.ts`) bring the suite to 111, all passing in the same isolated sandbox verification DEC-068 used (lint, both `tsc` configs, `vite build`, tests all clean); not yet re-confirmed on the operator's own machine. No real SerpApi credit has been spent by this work — the network path is tested only against a fake `fetch`, by design, pending the operator's own decision to run the first real search.

**DEC-063:** before running the AGENT_ARCHITECTURE.md step 4 shadow replay, direct inspection of `cache/phase5/horus.sqlite` found that DEC-062's fix does not fully cover it. Finescape and Sons' 3 evidence rows are all reachable with `evidenceBasePath` set to the repository root, exactly as DEC-062 verified. SEASONS EATS is different: its own discovery/listing row is one of 10 rows in that database with a NULL `id` and can never be read by `read_evidence_snapshot`; only 2 near-duplicate review-page snapshots for it have a real id, and both need `evidenceBasePath` set to `apps/operator`, not the repository root, to resolve. `scripts/run-shadow-replay.ts` (new; `npm run agent:shadow-replay -- finescape` or `... seasons`) takes a per-case `evidenceBasePath` and runs the SEASONS EATS side as an explicitly partial replay.

**DEC-067 approved by Javier Napoles, 2026-08-07, verified live (empty state):** `save_agent_draft` and `request_operator_review` — the last two of the original 4 unimplemented `ANALYST_TOOLS`, other than `run_deterministic_scoring` (still blocked on DEC-050) — are resolved without adding a write-capable MCP tool to the subprocess. `persistence.ts` gained an `agent_drafts` table; `runOpportunityAnalyst` (in `analyst-ipc.ts`) now accepts an optional `saveDraft` callback, invoked only after `parseAnalystOutput` has already accepted the result — never before, never with raw output. The panel shows a running list of saved drafts, each labelled "a record, not an approval or a state change." `request_operator_review` needed no new code at all: `AnalystOutput.proposedForReview` already is that signal, already schema- and evidence-validated. Verified with new `persistence.test.ts` and `analyst-ipc.test.ts` cases (draft saved only on success, exact payload asserted, never saved on failure or invalid output); not yet run live in the packaged app.

**DEC-066 approved by Javier Napoles, 2026-08-07:** `inspect_public_website_readonly` — the second of 4 unimplemented `ANALYST_TOOLS` — now has a real implementation (`electron/agent/website-inspector.ts`), registered as a second tool on the same MCP server `read_evidence_snapshot` uses. GET-only, https-only, a hostname denylist for the obvious local/internal cases, a 15s timeout, a 500KB body cap. Stated limitation: the hostname check is a literal-string denylist, not DNS-aware — it does not fully close SSRF via DNS rebinding or a redirect to a private address; documented as proportionate to a single-operator tool fetching URLs drawn from HORUS's own evidence, not solved. Verified with a real local HTTP server in `website-inspector.test.ts` (GET-only, rejection before any request, real truncation, real timeout). Wiring it in caught a stale test assertion in `agent-boundary.test.ts` (DEC-059's test hardcoded a single expected `--allowedTools` entry) — fixed, not a real defect; 80/80 tests pass, lint clean. Not yet verified live against an actual Claude Code run invoking the tool through `--mcp-config` — the existing kickoff prompt doesn't direct the analyst to use it. `run_deterministic_scoring` (blocked on DEC-050), `save_agent_draft`, and `request_operator_review` remain unimplemented.

**DEC-065 approved by Javier Napoles, 2026-08-07, verified live in the packaged app:** the analyst boundary is now reachable from the Electron app itself, not only from terminal scripts. `persistence.ts` gained a read-only `listRawSnapshots()`; `electron/agent/analyst-ipc.ts` (new) wires evidence selection through to a real `LocalAgentRuntime` run and validates the result with `parseAnalystOutput` before it can reach the renderer; `main.ts` exposes three IPC channels; `App.tsx` gained a collapsed-by-default "Opportunity analyst" panel — pick evidence, run, read the result. It saves nothing and makes no workflow-state transition (DEC-045); there is no "accept" button because nothing exists yet for one to write to. Unit-tested against fakes (`persistence.test.ts`, new `analyst-ipc.test.ts`) and confirmed live in the running app: the panel opened and correctly reported no retained evidence, with the rest of the representative workflow unaffected. `inspect_public_website_readonly`, `run_deterministic_scoring`, `save_agent_draft`, and `request_operator_review` remain unimplemented — this decision only reaches `read_evidence_snapshot`, the one tool that has ever had a real implementation.

**DEC-064:** both replays were then run live. Finescape (full evidence): the analyst read a 4.7/30 Maps rating, no `website` field, strongly positive reviews, and proposed the candidate for review — a qualitative read that does not match the retained historical outcome (retired at 48.1/100 by `reputation-scoring-v1`). This is expected, not a defect: the analyst never computes a score (rule 3) and only saw 3 snapshots, not the full retrieval the real deterministic model scored. It shows concretely why `reputation-scoring-v1` remaining unimplemented (DEC-050) matters — an agent's plausible qualitative read can diverge from the real model, and only the deterministic score is authoritative. SEASONS EATS (partial evidence, per DEC-063): the analyst proposed nothing, marking web-presence, full review population, and review text all `insufficient_data` — correct, evidence-honest abstention given a partial input, not a failure to reproduce the 73.06/100 approval it never had the evidence to see. AGENT_ARCHITECTURE.md step 4 is closed for both cases (one fully, one partially, as DEC-063 documents). Steps 5+ and the four unimplemented `ANALYST_TOOLS`/IPC-UI wiring remain open and unscheduled.

## Active phase

**Phase 0 — Definition: complete and approved** by Javier Napoles on 2026-08-05. Evidence: [`checkpoints/2026-08-05_phase-0-definition.md`](checkpoints/2026-08-05_phase-0-definition.md).

**Phase 1 — Calibration: complete and approved** by Javier Napoles on 2026-08-05. Evidence: [`checkpoints/2026-08-05_phase-1-calibration.md`](checkpoints/2026-08-05_phase-1-calibration.md).

**Phase 2 — Functional Design: complete and approved** by Javier Napoles on 2026-08-06. Evidence: [`FUNCTIONAL_DESIGN.md`](FUNCTIONAL_DESIGN.md) and [`checkpoints/2026-08-06_phase-2-functional-design.md`](checkpoints/2026-08-06_phase-2-functional-design.md). It defines behavior and interface review criteria only; it does not authorize implementation, publication, or outreach.

**Phase 3 — Technical Foundation: complete and approved** by Javier Napoles on 2026-08-06. Evidence: [`TECHNICAL_FOUNDATION.md`](TECHNICAL_FOUNDATION.md) and [`checkpoints/2026-08-06_phase-3-technical-foundation.md`](checkpoints/2026-08-06_phase-3-technical-foundation.md). Gmail recognized the final reserved-domain test recipient and HORUS did not invoke Send. A test-only Cloudflare Worker is public at `https://spring-night-6be6.javiernpls.workers.dev`; it contains no business data, contact, or operational capability. No HORUS credential, production demonstration, or outreach has been created.

**Phase 4 — First Vertical Workflow: complete and approved** by Javier Napoles on 2026-08-06. It validates the full local representative workflow with no external search, publication, Gmail opening, or contact.

Evidence: [`checkpoints/2026-08-06_phase-4-first-vertical-workflow.md`](checkpoints/2026-08-06_phase-4-first-vertical-workflow.md).

**Phase 5 — First Real Use: complete and approved** by Javier Napoles on 2026-08-06. An initial Finescape and Sons concept was retired when later review-history evidence scored 48.1/100, below the threshold. A subsequent SEASONS EATS run achieved a conservative lower-bound reputation score of 73.06/100, received the required operator judgment review, and was separately approved for local review and public deployment. Its public concept is `https://horus-seasons-eats-concept.pages.dev`, marked `noindex, nofollow` and without a form or contact route. A fresh public-data check preceded the approved Gmail compose handoff; the operator confirmed manual send. No email was sent by HORUS. The response and next follow-up remain pending tracked work.

Evidence: [`checkpoints/2026-08-06_phase-5-first-live-concept.md`](checkpoints/2026-08-06_phase-5-first-live-concept.md).

**Phase 6 — Validation and Hardening: pending.** Its proposed implementation sequence now includes correcting the foundation findings, then evaluating locally orchestrated agents through the operator's existing Claude subscription and Claude Code before considering a usage-metered model API. The agent runtime is documented but not implemented. Phase 6 begins in shadow mode and does not authorize additional public concepts or outreach by itself (DEC-045, `AGENT_ARCHITECTURE.md`).

**2026-08-08 partial checkpoint:** a full real pipeline — discovery, G1/G2 screening, review-history and reputation scoring, partial web-opportunity measurement, proximity, shortlist ranking, prospect selection, discovery caching, website screenshot capture, and a demonstration-site preview — is built and mostly live-verified (DEC-068 through DEC-079). Evidence: [`checkpoints/2026-08-08_phase-6-real-mvp-pipeline.md`](checkpoints/2026-08-08_phase-6-real-mvp-pipeline.md).

**2026-08-08 third partial checkpoint, same day:** the real pipeline ran end to end for the first time — a real search through to a demonstration preview, on live business data, stopping before both DEC-004 gates. Evidence: [`checkpoints/2026-08-08_phase-6-first-real-pipeline-run.md`](checkpoints/2026-08-08_phase-6-first-real-pipeline-run.md).

**2026-08-08 second partial checkpoint, same day:** real Cloudflare publication, real outreach composition with a Gmail handoff, and the tracker are all built — charter §4's full loop (search through tracked follow-up) now exists end to end for the first time (DEC-080 through DEC-082). Evidence: [`checkpoints/2026-08-08_phase-6-full-loop-complete.md`](checkpoints/2026-08-08_phase-6-full-loop-complete.md). Nothing in either checkpoint has been exercised against real infrastructure beyond DEC-073; no public concept or contact is authorized.

### Phase 6 working draft — written 2026-08-07, not approved and not executed

Steps 1 to 3 of the validation sequence are **written and verified**, statically and at runtime, on macOS on 2026-08-07: oxlint clean, 52 of 52 tests passing, `tsc -b`, `vite build` and `tsc -p tsconfig.electron.json` clean, the DEC-047 migration executed and confirmed by inspecting the schema, and a workflow save recorded end to end.

The agent runtime contract was checked against Anthropic's published CLI documentation (DEC-056). The analyst task is schema-constrained through `--json-schema`, so prose is refused rather than accepted, and `session_id`, `total_cost_usd` and `num_turns` are recorded per run. That review found a conflict with DEC-045: `--bare`, the recommended mode for scripted calls, bypasses the subscription login and requires an API key, so HORUS does not use it — which otherwise would have meant Claude Code auto-discovering this repository's own `CLAUDE.md` on every analyst run.

That gap is now closed (DEC-057): `--system-prompt` replaces the default system prompt with the task's own instruction, and every run gets a fresh, empty working directory created under HORUS's own data directory, confirmed on creation to contain no `CLAUDE.md` or `.claude`.

**The operator then ran the exact command HORUS constructs, for the first time, from an isolated directory.** It succeeded: `structured_output`, `session_id` and `total_cost_usd` all arrived exactly as DEC-056 predicted from documentation, confirming the runtime contract against a live process. It also surfaced DEC-058 — Claude Code attempted a tool call in a run that supplied none, revealing that `task.allowedTools` had only ever been validated as data, never passed to Claude Code's actual permission surface. `--permission-mode dontAsk` contained the gap but was not the designed control.

**DEC-059 closes the first piece of that gap.** `read_evidence_snapshot` — one of `ANALYST_TOOLS`' five names — now has a real implementation: a stdio MCP server (`electron/agent/evidence-mcp-server.ts`) built on the officially supported `@modelcontextprotocol/sdk` v1.x, backed by a database connection opened with better-sqlite3's `readonly: true` (`electron/agent/evidence-store.ts`), proven to reject writes in a unit test against a real handle. `buildClaudeCodeArgs` now allow-lists exactly this tool when wired. The other four `ANALYST_TOOLS` names remain unwired and still resolve to no access, per DEC-058.

**Verified live.** `npm install` pulled `@modelcontextprotocol/sdk` and `zod` cleanly, 61 of 61 tests pass, and the operator then ran `claude -p` with `--mcp-config` pointing at the compiled server against a real, hand-seeded row in `horus.sqlite`. Claude Code called `read_evidence_snapshot` and returned the row's exact contents — data it had no way to produce except by reading it, with `permission_denials: []` confirming the DEC-059 allow-list worked as designed. This is the first tool in `ANALYST_TOOLS` to go from name, to code, to a live call, in one session.

What's left before a real analyst task can run end to end: this test used a hand-built prompt and a single seeded row, not `buildAnalystTask`'s actual instruction, schema, and evidence references together with the tool active. That combined run — schema-constrained output *and* a working tool in the same invocation — is next.

**DEC-060 found that gap was one level deeper than expected.** Every verification so far — the schema, the isolation, the permission lockdown, the tool call — used either a test fake or the operator typing `claude -p` directly into a terminal. No code in the repository had ever actually launched Claude Code; `SpawnImpl` had no real implementation. `electron/agent/node-spawn.ts` is that implementation now, tested against real subprocesses including a timeout kill and an argument-injection check. `npm run agent:live-check` assembles the full path — real evidence seeded, real task built by `buildAnalystTask`, real tool wired, real spawn, real isolated directory — and checks the result with `parseAnalystOutput`. **It found a real bug on the first run, and passed cleanly on the second (DEC-061).** The first run showed Claude Code had guessed nonexistent evidence-snapshot ids, because `buildKickoffPrompt` said how many snapshots existed but never named which ones. Fixed, and rerun: given the real ids, Claude Code called `read_evidence_snapshot` against both, produced three evidence-cited observations (the listing's 4.6 rating and 212 reviews, and PageSpeed's 41/11.2s performance figures — reported as observed facts, never as a score field), correctly marked category, location, and contact information as `insufficient_data` rather than guessing, and proposed the candidate for review with a rationale citing both snapshots.

**This is the first fully live, end-to-end confirmation of `AGENT_ARCHITECTURE.md` step 3.** Real task built by `buildAnalystTask`, real evidence tool, real spawn (`node-spawn.ts`, DEC-060), real isolated working directory, schema-constrained output, and HORUS's own `parseAnalystOutput` acceptance check — all operating together, against evidence the analyst had never seen before, not a hand-built prompt or a single hand-seeded row.

**Before attempting step 4, checked whether the real Finescape and Sons and SEASONS EATS evidence the roadmap names actually exists anywhere reachable.** It does — `cache/phase5/horus.sqlite`, a separate database from the one the Electron app writes, holding 64 real rows across discovery, reviews, PageSpeed, and manual website-analysis sources. But its `storage_path` values are relative to the repository root, in two different spellings, and the isolated directory DEC-057 deliberately runs the evidence server from is never the repository root — so reading this real evidence would have failed the moment step 4 tried it. DEC-062 adds a `basePath` to `evidence-store.ts`, consulted only for a relative path; every snapshot HORUS's own write path has ever produced is already absolute, so nothing about normal operation changes.

**Proven twice: a unit test, then the real file.** The operator ran `claude -p` from `/tmp/horus-finescape-check` — sharing nothing with the repository — pointed at the actual `cache/phase5/horus.sqlite`. It correctly reported "Position 11: Finescape and Sons," reading the real historical listing from its original, relative-path record. The evidence step 4 needs exists, is real, and is now reachable from an isolated agent run.

Getting there required four corrections that were not in the Phase 6 plan, because **the application had never actually run before**:

- The Electron build emitted one directory too deep, breaking both the dev script and the renderer path (DEC-051).
- The preload compiled as an ES module, which Electron's sandboxed loader rejects. `window.horus` was undefined and every save was a silent no-op (DEC-052).
- The dev server bound to IPv6 while Electron requested IPv4 (DEC-055).
- The store writes to Electron's generic application directory, and the renderer has no Content-Security-Policy (DEC-054, recorded without remedy).

Two consequences worth carrying forward:

- **`horus.sqlite` did not exist until today.** The Phase 1 calibration evidence lives in `cache/raw/serpapi/google_maps/` as 362 timestamped files. `appendRawSnapshot` had only ever run inside tests, so the persistence layer described in earlier checkpoints was implemented and tested but had never been the system of record.
- **The interface advances independently of the record.** While the preload was broken, the workflow was walked from stage 01 to stage 07 — past the demonstration approval gate — with nothing persisted at all. That is a defect in `App.tsx`, not in the preload, and it is recorded in DEC-053 with the remedy deliberately deferred.

Written:

- The three foundation corrections: credential-free provenance (DEC-046), one snapshot row per retrieval with a `user_version` migration (DEC-047), and main-process validation of renderer-submitted workflow state (DEC-048).
- The provider-neutral agent boundary and the single bounded analyst task, with the section 11 acceptance criteria enforced in the output parser (DEC-049).
- Twenty new automated tests — two for the integration and persistence corrections, seven for main-process state validation, eleven for the agent boundary — bringing the suite from 25 to 45 if they pass as written.

Not written, and still required before Phase 6 can be called complete:

- Verification of the Claude Code command shape, flags, authentication behaviour and error wording against a real installation. `classifyFailure` currently matches on strings nobody has observed (`AGENT_ARCHITECTURE.md` section 2).
- Steps 4 to 8: the Finescape and SEASONS EATS shadow-mode replay, the comparison against the operator's historical decisions, and the retain/revise/reject decision on the runtime. All of these require execution.
- Wiring the agent boundary into an IPC handler and the interface. The modules exist and are tested in isolation; nothing calls them yet.

Known consequence to check on first run: DEC-047 changes `rawSnapshotCount` from a count of distinct payloads to a count of retrievals, so the foundation status number will move. The migration rewrites a table holding real Phase 1 calibration evidence and should be run against a copy of the database first.

A separate finding, outside Phase 6 scope: `reputation-scoring-v1` was specified in charter section 9 but implemented nowhere in the code (DEC-050); `src/domain/reputation-scoring.ts` now closes that specific gap (DEC-068, proposed, pending operator review) — but the calibration and SEASONS EATS reputation figures are still not reproduced from this repository, since the original per-business raw inputs have not yet been re-extracted and run through it.

## Completed

- [x] Create an independent repository for HORUS V1.
- [x] Create the initial documentation structure.
- [x] Define HORUS as the company and HORUS V1 as an internal operating tool (DEC-003).
- [x] Define the primary user: the founder, sole operator.
- [x] Define the primary workflow end to end, from category and area through to a tracked opportunity.
- [x] Define the hard constraints: two blocking approval gates (DEC-004) and no fabricated content (DEC-005).
- [x] Define the V1 out-of-scope list and initial success criteria.
- [x] Define reputation qualification: six minimum gates, a five-factor 100-point model (`reputation-scoring-v1`), and a 70-point qualification threshold (charter section 9).
- [x] Choose Google reviews as the sole V1 reputation source (DEC-006).
- [x] Split rejection into objective auto-rejects and judgment-based operator flags, with flags never rejecting automatically (DEC-008).
- [x] Define evidence-retention requirements so any scoring run is reproducible as a historical snapshot.
- [x] Establish that web presence scores rather than filters; all three presence situations are eligible (DEC-009).
- [x] Define web opportunity as a 100-point model, with obsolete appearance grouped as one signal (DEC-010, DEC-011).
- [x] Adopt PageSpeed Insights as the measurement source for site performance, with the Lighthouse mobile profile as the fixed profile (DEC-012).
- [x] Define shortlist ranking: reputation qualifies, web opportunity ranks, reputation breaks ties (DEC-013, charter section 11).
- [x] Define search scope: run to `TARGET_QUALIFIED`, bounded by `MAX_EXAMINED`, both operator-set; standards never relaxed to fill the quota (DEC-014, charter section 12).
- [x] Express the search area as a city name, with resolution and boundary caveats recorded (DEC-015).
- [x] Add proximity as a third ranking dimension, measured from a stored home base, ranked in bands (DEC-016, DEC-017 — supersedes DEC-013).
- [x] Choose SerpApi for candidate discovery and review history, closing the review-data risk (DEC-018, charter section 13).
- [x] Establish the credit cost model and accept the free tier for the first real use (DEC-019).
- [x] Require local caching of all external responses, with immutable raw data and recomputable derived scores (DEC-020, charter section 14).
- [x] Set a 30-day maximum data age at the point of contact, enforced at the approval gates rather than at search time (DEC-021).
- [x] Choose Cloudflare Pages on a free `pages.dev` subdomain as provisional deployment, with a HORUS domain as the expected migration (DEC-022).
- [x] Define the demonstration as a real navigable website with explicit limits, mandatory publication requirements, and an image-sourcing rule (DEC-023, DEC-024, DEC-025, charter section 15).
- [x] Set the first target market as Stamford and Norwalk, Connecticut, with distances in miles (DEC-026).
- [x] Set the language policy: English by default, Spanish only on recorded evidence, operator-overridable (DEC-027).
- [x] Decide outreach delivery: HORUS hands an approved message to Gmail without API credentials; the operator composes and sends (DEC-041, supersedes DEC-028).
- [x] Establish that HORUS V1 is an application with a visual interface and persistent state (DEC-029).
- [x] Resolve in-person follow-up: an operator action logged in the tracker after a prospect shows interest, not a workflow HORUS runs (DEC-030).
- [x] Set demonstration lifetime: at 60 days without a response HORUS prompts the operator to decide on removal; nothing is removed automatically (DEC-031).
- [x] Record the Phase 0 checkpoint and restructure the ROADMAP around calibration.

- [x] Close all three remaining charter questions: data budget, web-presence ordering, commercial ineffectiveness (DEC-032, DEC-033, DEC-034).
- [x] Extend the web model to `web-opportunity-v2` with a fifth factor and rebalanced weights.

- [x] Sign the charter — approved by Javier Napoles, 2026-08-05, first authorized phase: Phase 1.
- [x] Set the home base in Stamford CT, stored outside version control (DEC-035).
- [x] Initialize the Git repository with a `.gitignore` protecting the operator configuration.
- [x] Complete Phase 1 calibration and retain both scoring-model versions after operator review.
- [x] Complete Phase 2 functional design: ten-step behavior, six views, evidence standard, approval gates, and the V1 demonstration-template baseline.
- [x] Establish the Phase 3 local Electron/React/TypeScript application foundation, typed preload boundary, SQLite store, immutable raw-evidence manifest, and append-only event log (DEC-042).
- [x] Add automated workflow, compose-handoff, persistence-invariant, and non-production integration-contract tests; local build and lint checks pass.
- [x] Add credential-safe contracts for SerpApi, PageSpeed, Gmail compose handoff, and Cloudflare Dashboard upload; no contract performs a network call.
- [x] Validate Cloudflare Dashboard Direct Upload with a 939 B, test-only static HTML asset; deployed without Wrangler or a Cloudflare API token (DEC-043).
- [x] Execute a real, explicitly approved 20-result landscaping search in Stamford and retain the returned raw evidence locally; the discovery request uses the documented SerpApi Maps query form with the city embedded in `q`.
- [x] Add a main-process-only SerpApi discovery executor and an automated contract test proving that the API key is never returned in the payload.
- [x] Publish and subsequently retire the Finescape and Sons concept after a documented reputation score below threshold; the retained Cloudflare Pages project now serves no business content.
- [x] Complete the first qualified real prospect run with SEASONS EATS: evidence-gated qualification, separately approved public concept, fresh outreach evidence, Gmail draft handoff, operator-confirmed manual send, and pending follow-up record.
- [x] Record the Phase 6 agent-runtime direction: evaluate a locally authenticated, subscription-backed Claude Code process behind a provider-neutral main-process boundary, without adding usage-metered model API billing to the initial pilot (DEC-045).

## Phase 1 evidence

**Phase 1 — Calibration, complete.**

The controlled calibration retrieval covered restaurants in Stamford, plumbing in Stamford, and landscaping in Norwalk. Restaurant discovery returned 60 rows representing 58 unique businesses; 37 passed G1/G2. Plumbing returned 13 G1/G2 survivors, of which 8 have a verified Stamford address; one is in Greenwich and four lack a verifiable address. Landscaping returned 10 G1/G2 survivors, of which 9 have a verified Norwalk address; one is in Wilton. The selected calibration set has 30 businesses: 13 restaurants stratified by review volume, 8 plumbers, and 9 landscapers.

Every selected business has cached raw candidate and initial-review responses. Thirty of the review histories have been paginated as needed through up to three pages: 21 cross the 365-day boundary, one has no further page and is complete within the data available, and 8 remain `partial_data` with a further page available. A test history jumped from June 2026 to February 2024 on its second page, so a pagination gap is never interpreted as proof that reviews are absent. The current decision is to stop before further pagination and assess the cost pattern. Raw responses and HTTP headers are cached locally outside version control.

Cost reconciliation, 2026-08-05: the SerpApi account balance is the billing source of truth and reports 159 of 250 free searches remaining; calibration has therefore consumed 91 credits (36.4% of the monthly allowance). The raw cache contains 92 successful payloads and 23 invalid payloads, so a raw-success count must not be treated as a debit ledger; the invalid pagination requests did not consume a credit. Thirty-two successful payloads are page-2 or page-3 review continuations. Completing the next available page for each of the 8 partial histories is bounded at 8 additional credits, but is not needed to preserve the current conservative lower-bound scores and will not be spent without an explicit decision. The 91-credit observed calibration is within DEC-032's 100–155 one-time calibration allowance; it does not validate the separate ~48-credit estimate for a normal five-prospect run because this exercise deliberately retrieved 30 businesses across three categories and included diagnostic requests.

A provisional reputation calculation under `reputation-scoring-v1` is complete, without changing any parameter. Of the 22 complete histories, 8 fail G3 on complete data and are auto-rejects; 14 pass G3, of which 6 score at least 70. Their score range is 49.5–85.1, with a median of 67.0. The 8 partial histories all already prove G3; scored conservatively with Factor 4 at zero and Factor 5 as `longevity_unknown`, two have a lower bound at or above 70. These results are calibration evidence, not final prospect qualification or a threshold decision.

Review-volume saturation check: the selected listings span 28–6,326 published reviews; 12 have fewer than 100 reviews, 6 have 100–399, and 12 are at or above the 400-review Factor 2 plateau. The boundary is represented by Tuff Lawn (314 reviews, 22.8 Factor-2 points), CINCO DE MAYO (392, 24.8), and Teed and Brown (435, 25.0), showing the logarithmic curve reaches the plateau smoothly rather than creating a large boundary jump. The many plateaued businesses confirm that volume beyond 400 should not be treated as a proxy for commercial maturity; the existing operator-flag treatment remains appropriate. This sample alone does not justify changing the 400-review parameter or creating a new model version.

Factor-4 floor check: 23 of the 30 selected businesses have at least five retrieved trailing-year reviews and can receive a consistency score. Fourteen receive the full 15 points, 9 receive a reduction for a negative delta, and 7 do not meet the input minimum. The full-credit floor is therefore common but not automatic in the observed set. The reduced cases include CINCO DE MAYO (2.4 Factor-4 points), Wonder Stamford (1.5), Teff (5.2), and Mecha Noodle Bar (8.8); this shows that measured decline is already capable of materially restraining otherwise strong listings. The evidence does not show that Factor 4 systematically makes the reputation gate too easy, so no parameter change is justified yet.

Manual boundary review, 2026-08-05: the operator would approach **CINCO DE MAYO** (68.3, complete data) and **United Sewer & Water** (65.6, complete data), but would not approach **Tuff Lawn** (73.6, complete data), **Mashed Burgers** (68.3, partial), **Barcelona Wine Bar** (65.4, complete data), or **FAIRCONN Plumbing** (65.0, partial). The stated reasoning: CINCO DE MAYO has a Wix site that is not responsive and a poor-quality logo; United has a poor design despite being responsive and offering a chatbot; Tuff Lawn appears to be a more consolidated company. In this small review, the 70-point threshold creates two false negatives and one false positive. The first two rationales are web-opportunity signals rather than reasons to weaken reputation qualification; the third is a judgment-dependent maturity signal, so it belongs as an operator flag rather than an auto-reject. This is evidence to examine the threshold and factor behavior; it is not sufficient to change a versioned model without a broader review and explicit decision.

Additional manual calibration review, 2026-08-05: the operator agrees that **TwoGen Landscaping** and **JNR Plumbing** should not be approached despite their stronger model signals; both present mature, complete local-service websites and are poor web-opportunity cases. **A Plus Ornamental & Turf Specialists** is retained as a `maybe`: its site is functional but has weaker copy quality, 46 reviews, and mobile performance 62. **LT Landscaping & Masonry** is not an approach candidate: it has no website URL in the listing but its weaker reputation evidence does not make absence of a site sufficient reason to pursue it. This supports the intended separation: web opportunity ranks reputation-qualified candidates, while commercial maturity is an operator judgment and a no-site state does not override weak qualification.

Direct site inspection adds evidence to the boundary review. CINCO DE MAYO is publicly hosted on a Wix subdomain and displays Wix branding; its visible phone-number link routes to a Google search rather than `tel:`, so it is a broken contact path under Factor 3. The operator reports that the site is not responsive. United presents working `tel:` links, service descriptions, a service-scheduling CTA, and a chat widget, so its opportunity is visual rather than a missing-contact-path case. Its retained COVID-safety link is a possible dated-content indicator, but not enough to score obsolete appearance on its own.

PageSpeed check, 2026-08-05: the initial unauthenticated mobile request returned HTTP 429 because the default consumer has a daily quota of zero. PageSpeed Insights was then enabled in the HORUS Google Cloud project and a locally stored key was created with an API restriction to PageSpeed Insights only. The fixed mobile Lighthouse responses and headers are cached locally. CINCO DE MAYO scores 84 performance (FCP 2.41 s, LCP 4.02 s, TBT 0 ms, CLS 0); United scores 61 (FCP 3.01 s, LCP 14.48 s, TBT 286 ms, CLS 0.00004). This independently supports a substantial performance opportunity for United and lets Factor 4 and the Lighthouse-derived portion of Factor 1 be evaluated.

The complete PageSpeed pass covers 28 of 29 public website URLs in the 30-business calibration set: valid mobile performance scores range from 14 to 87, with a median of 60.5; 13 are below 60 and 16 have LCP above 10 seconds. LT Landscaping & Masonry has no website URL in its discovery record, while Wonder Stamford returned no PageSpeed response after two attempts with a 120-second cap, so neither receives an invented performance score. Repeated runs can vary materially (EarlyGreen measured 59 and 51 on separate runs), therefore these are lab-run calibration evidence rather than stable production claims. The results reinforce the need to keep business maturity as an operator flag separate from web opportunity: Tuff Lawn appears commercially consolidated but measured 21 performance, LCP 25.2 s, and CLS 0.92.

## Next

Phase 6 may review and harden the process from the completed SEASONS EATS run. It should first correct the credential, historical-snapshot, and main-process validation findings, then implement one bounded Claude Code analyst task and replay Finescape and SEASONS EATS in shadow mode. The pending SEASONS EATS response should be recorded when the operator reports it. Sunshine Cuisine and Caribbean Bakery & Mini Mart remain separate discovery candidates; reputation, fresh evidence, explicit approvals, and separate real-use authorization remain required before any new outreach.

## Blockers

**No current technical blocker.** Wrangler 4.119.0 was authenticated through the operator's Cloudflare OAuth flow and used as a one-off direct Pages deployment client; no Wrangler dependency or credential was added to the repository. Cloudflare cannot delete an active production deployment, so the Finescape concept was retired by replacing it with a neutral static page while retaining the Pages project. The SEASONS EATS outreach was sent manually by the operator after a Gmail draft handoff.

## Known limitations carried forward

These do not block starting. They are what calibration exists to resolve, and they should be watched deliberately rather than discovered by surprise.

**Untested numbers**

- The 70-point threshold, 400-review saturation point, and Factor-4 floor were tested in one representative calibration set and retained, not proven universally correct (DEC-007).
- `NO_SITE_BASE` at 50 was observed once; `SOCIAL_ONLY_BASE` at 60 remains unobserved (DEC-033).
- The 5 / 15 / 30-mile driving bands, and whether distance should eventually yield to travel time.
- The ~48-credit estimate for a normal five-prospect run remains unmeasured; the 91-credit calibration was deliberately broader and is not comparable.
- Whether proximity-first ranking costs more in response rate than it gains in convenience (DEC-030).

**Undefined capabilities**

- Detecting non-functional buttons and dead links requires executing pages; PageSpeed Insights does not cover it.
- Franchise and centralized-marketing detection has no rule, though both are operator flags rather than auto-rejects.
- Whether a city search follows administrative or metropolitan boundaries (DEC-015).
- PageSpeed Insights usage limits are unverified, and field data is typically unavailable for small local businesses.

**Accepted trade-offs**

- Send status is operator-declared, not observed (DEC-041).
- Demonstration removal depends on the operator answering a repeated prompt (DEC-031).
- A free-hosting URL carries less credibility in cold outreach than an established domain (DEC-022).
- Factor 5 measures absences, which are easy to detect and easy to get wrong when content exists where HORUS did not look (DEC-034).

**Not yet designed**

- Production deployment automation is not selected; the Phase 3 Cloudflare Worker was test-only and does not replace the future Pages project.

## Technical status

| Area | Status |
| --- | --- |
| Application code | Electron/React foundation implemented and now buildable; never launched (DEC-051). `reputation-scoring-v1` now has real code (DEC-068) alongside `web-opportunity-v2`, now itself partially real (2 of 5 factors: DEC-072). Real SerpApi discovery is reachable from the interface (DEC-069), with a G1/G2 quick screen per candidate (DEC-070), real per-candidate review-history and reputation scoring (DEC-071), real per-candidate performance/https measurement (DEC-072), real proximity (DEC-074), real shortlist ranking (DEC-075), a consolidated, in-memory-only prospect-selection view (DEC-076), a corrected `maxExamined` cap plus discovery result caching (DEC-077), an on-request website screenshot in the prospect record (DEC-078), a real, unpublished demonstration-site preview generator (DEC-079), real Cloudflare Pages publication behind a DEC-004 approval checkbox (DEC-080), real outreach composition with a Gmail handoff behind the second DEC-004 gate (DEC-081), and a tracker projecting the durable event log into a follow-up pipeline (DEC-082) — charter §4's full loop now exists end to end, search through tracked follow-up |
| Tests | 381 of 381 automated tests passing as of 2026-08-09 (190 plus 25 DEC-083 contrast/token checks, 14 DEC-084/DEC-085 accessibility checks, 10 DEC-086 calibration-reproduction checks, 7 DEC-087 Phase 5 reproduction checks, 12 DEC-088 security checks, 26 DEC-089 freshness checks, 8 DEC-090 removal checks, and 13 DEC-091 operator-judgment checks, plus 10 DEC-093 DOM interaction checks and 14 DEC-094 judgment-log checks, plus 18 DEC-095 refresh checks and 15 DEC-096 demonstration-review checks, plus 19 DEC-097 mobile-responsiveness checks), lint and both builds clean on the operator's own macOS machine; the real discovery/reputation/web-opportunity paths are confirmed live in the packaged app spending real credits (DEC-073); proximity (DEC-074), shortlist ranking (DEC-075), prospect selection (DEC-076), discovery caching (DEC-077), screenshot capture (DEC-078), the demonstration preview (DEC-079), real publication (DEC-080), real outreach (DEC-081), and the tracker (DEC-082) are not yet live-verified |
| Infrastructure | SQLite store implemented and tested but never used; retained evidence lives in `cache/` as plain files |
| Integrations | Non-production contracts implemented; Gmail compose handoff exercised with operator-confirmed manual send |
| Agents | Boundary and one bounded analyst task written and unit-testable; runtime unverified against a real Claude Code installation, and nothing calls it yet |
| Design | Functional design approved. Visual baseline superseded in part 2026-08-08: DEC-036's principles and exclusions stand, its "restrained editorial" register and implied light luminance are replaced by DEC-083's dark instrument-panel register under six binding rules. Token contrast is machine-verified (`tests/contrast.test.ts`), partly discharging DEC-038's deferred verification method for colour. The demonstration generator keeps its own light stylesheet and shares no tokens (DEC-083 rule 6) |
| Deployment | Cloudflare Pages direct deployments verified for bounded public concepts |

## Update rule

Update this file at the end of every significant work session. In under two minutes, it should explain what exists, what remains, and which next step is authorized.
