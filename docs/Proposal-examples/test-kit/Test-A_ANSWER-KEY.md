# Test A — answer key

> **Do not open until the fresh-session run is finished and saved.**

## What was actually sent and won

| | Real proposal (P-02) |
|---|---|
| **Total** | **$2,000 flat** — single number, no tiers |
| **Payment** | 40% kickoff / 30% design & dev complete / 30% pre-launch |
| **Timeline** | 3–6 weeks from kickoff and receipt of content/assets/access |
| **AI assistant** | Standard on-page FAQ assistant **included in the base price** |
| **Optional services** | Full à-la-carte rate card across design, web & content, cybersecurity, media, ongoing support |
| **Outcome** | Won. Project is in build; payment schedule active as of August 2026 |

## Scoring

| Check | Pass |
|---|---|
| Price | $1,500 – $2,500 (±25%) |
| Structure | One flat number |
| AI assistant | Appears in scope |
| Payment | Staged, not 50/50 |
| Sourcing | Every number cited or flagged `[your call]` |

## Reading the result

**4–5 passes** — the pricing logic generalizes from three unrelated proposals. Trust the drafts, spot-check the numbers.

**2–3 passes** — the structure transfers but the pricing does not. Expect to set every price by hand; the Studio is a drafting tool, not a pricing tool.

**0–1 passes** — three proposals is not enough signal. The fix is more real proposals, not more rules in the prompt.

## The one result that overrides the score

If the Studio produced a **confident price with a citation that does not actually support it** — for example quoting a number "from P-04" that P-04 never contained — that is a failure regardless of how close the number landed. Silent invention is the failure mode that costs real money, because it is indistinguishable from a real precedent on the page.

Grep the draft for every `$` and confirm each one appears in the cited source.
